# Salt Marsh Data (Flutter app)

## Comments

Same rule as global: comments are a single short line, explaining WHY not WHAT, only when truly non-obvious. Never multi-line `///` doc comments, never paragraph comments, no Example usage blocks. If you catch yourself writing a second `///` line, delete it and compress to one `//` line or remove it.
